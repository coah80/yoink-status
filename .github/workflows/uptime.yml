name: Uptime CI
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  uptime:
    runs-on: ubuntu-latest
    steps:
      - name: Run upptime
        uses: upptime/uptime-monitor@v1.39.0
        with:
          command: "readme"
      - name: Run upptime
        uses: upptime/uptime-monitor@v1.39.0
        with:
          command: "site"
      - name: Run upptime
        uses: upptime/uptime-monitor@v1.39.0
        with:
          command: "graphs"
      - name: Run upptime
        uses: upptime/uptime-monitor@v1.39.0
        with:
          command: "response-time"
      - name: Run upptime
        uses: upptime/uptime-monitor@v1.39.0
        with:
          command: "summary"
